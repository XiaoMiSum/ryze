# rabbitmq 默认配置，各配置项的优先级为：取样器 > 默认配置
testclass: rabbit # 配置元件类型
config:
  url: 'amqp://guest:guest@localhost:5672/'
  timeout: 5000 # 连接超时时间，毫秒，默认值：60000
  queue: # 队列配置
    name: ryze.topic  # 消息队列名称
