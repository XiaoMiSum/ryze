title: proto in websocket # 包含配置原件、前置处理器、后置处理器、多个取样器（执行步骤）
configelements:
  - !include '配置元件/proto_ws.yaml'
children: # 执行步骤
  - title: '测试步骤1'
    testclass: proto
    config:
      path: '/user'
      body:
        name: name_2
        age: 18
        id: 1
      response_pattern: name
    validators:
      - { testclass: json, field: '$.name', expected: 'name_2' }